{
  "id": "001-add-health-endpoint",
  "title": "Add a /health endpoint",
  "repo": "self",
  "prompt": "Add a /health endpoint to the proxy HTTP server that returns {ok: true, version: <package.json version>}. Include a vitest test.",
  "assertions": []
}
