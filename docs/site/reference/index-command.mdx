---
title: index
description: "Build local code intelligence graph (replaces 'pull' in Local Mode)"
---

Build local code intelligence graph (replaces 'pull' in Local Mode)

```bash
unerr index [options]
```

**Options**

| Flag | Description | Default |
| --- | --- | --- |
| `--force` | Re-index even if local snapshot is fresh | — |
| `--verbose` | Show per-file indexing progress | — |
| `--json` | Output results as JSON (to stdout) | — |
