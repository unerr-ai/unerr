---
title: discover
description: "Surface commands that bypass effective compression — untapped savings"
---

Surface commands that bypass effective compression — untapped savings

```bash
unerr discover [options]
```

**Options**

| Flag | Description | Default |
| --- | --- | --- |
| `-n, --limit \<n\>` | Number of rows to show | `20` |
| `-t, --threshold \<pct\>` | Ratio floor (0..100) below which a command is flagged | `30` |
| `-m, --min-raw \<bytes\>` | Ignore commands with raw output below this | `2000` |
