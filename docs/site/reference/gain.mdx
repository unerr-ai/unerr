---
title: gain
description: "Show top commands by tokens saved through shell compression (this repo)"
---

Show top commands by tokens saved through shell compression (this repo)

```bash
unerr gain [options]
```

**Options**

| Flag | Description | Default |
| --- | --- | --- |
| `-n, --limit \<n\>` | Number of rows to show | `20` |
